cmake_minimum_required(VERSION 3.3 FATAL_ERROR)
project(glmath VERSION 0.1 LANGUAGES CXX)

add_library(glmath INTERFACE)
target_include_directories(glmath INTERFACE include)

set(glmath_headers
    include/glmath.h
)

add_custom_target(glmath_ide SOURCES ${glmath_headers})

add_executable(test1 tests/sum.cpp)
target_include_directories(test1 PRIVATE ${CATCH_INCLUDE_DIR})

enable_testing()
add_test(test1 test1)
